<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="SqlSaturday.Views.SessionsPage"
    Title="{Binding Title}"
    BackgroundColor="{StaticResource DarkBackgroundColor}">
    <ContentPage.Content>
        <ListView
            x:Name="sessionsListView"
            BackgroundColor="{StaticResource DarkBackgroundColor}"
            ItemsSource="{Binding GroupedSessions}"
            IsGroupingEnabled="true"
            GroupDisplayBinding="{Binding Key}"
            SeparatorVisibility="None"
            HasUnevenRows="true"
            RefreshCommand="{Binding LoadSessionsCommand}"
            IsPullToRefreshEnabled="true"
            IsRefreshing="{Binding IsBusy, Mode=OneWay}"
            CachingStrategy="RecycleElement">
            <ListView.GroupHeaderTemplate>
                <DataTemplate>
                    <ViewCell Height="25">
                        <StackLayout VerticalOptions="FillAndExpand"
                               Padding="5"
                               BackgroundColor="#3498DB">
                            <Label Text="{Binding Key}" TextColor="White" VerticalOptions="Center"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.GroupHeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout 
                            BackgroundColor="#282E33" 
                            Orientation="Vertical"
                            Padding="5">
                            <StackLayout
                                BackgroundColor="#3E4549"
                                Orientation="Horizontal"
                                Padding="35, 20, 5, 20"
                                Margin="25, 5, 5, 5">
                                <StackLayout
                                    Orientation="Vertical"
                                    HorizontalOptions="StartAndExpand">
                                    <Label Text="{Binding Track}" LineBreakMode="NoWrap" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="13"/>
                                    <Label Text="{Binding Title}" LineBreakMode="NoWrap" Style="{DynamicResource ListItemTextStyle}" FontSize="16"/>
                                    <Label Text="{Binding DisplayTime}" LineBreakMode="NoWrap" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="13"/>
                                    <Label Text="{Binding Room}" LineBreakMode="NoWrap" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="13"/>
                                    <Label Text="Speaker Name" LineBreakMode="NoWrap" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="13"/>
                                </StackLayout>
                                <Label 
                                    Text=">" 
                                    HorizontalOptions="End"
                                    Style="{DynamicResource ListItemTextStyle}" 
                                    FontSize="16"/>                                
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
